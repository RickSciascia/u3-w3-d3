const initialState = {
  fav: {
    content: [],
  },
};

const mainReducer = function (currState = initialState, action) {
  switch (action.type) {
    // case
    // case
    default:
      return currState;
  }
};

export default mainReducer;
